{
  "buildCommand": "cd frontend && npm install && VITE_DEMO_MODE=true npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
